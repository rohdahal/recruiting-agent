# Portfolio Copy Snippets

## 1-line project summary

Built a local-first AI recruiting workflow that ingests Word resumes, runs RAG-based candidate retrieval, ranks fit with transparent scoring, and auto-generates outreach drafts.

## Resume bullet (short)

- Built an AI recruiting workflow prototype (TypeScript/React/Node) with DOCX resume RAG, candidate ranking, and local LLM outreach generation via Ollama.

## Resume bullet (impact-oriented)

- Designed and shipped a local-first recruiting agent pipeline that transforms a role prompt into a ranked shortlist + personalized outreach, with parser confidence gates and explainable tool traces.

## LinkedIn project description

I built an AI Recruiting Workflow Experiment to explore how LLM agents can automate sourcing and outreach without black-box behavior. The system parses `.docx` resumes, retrieves relevant candidates via RAG, applies a transparent ranking model, and drafts personalized outreach messages. Everything runs locally with Ollama, and the app includes parser quality scoring plus fallback paths for reliability.
